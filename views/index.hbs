<div class="gradient-background">
    {{#if isApproved}}
    {{> header }}
    <div class="container">
        <div class="all-post-container">
            {{#posts}}
            <div class="post-main-block post-container" id="{{post_id}}">
                <div class="post-top-blck">
                    <div class="img-block">
                        {{#each user_details}}
                        {{#each user_profile}}
                        <img class="handled-profile-picture" src="/images/{{this.profilepic}}" alt="" />
                        {{/each}}
                        {{/each}}
                    </div>
                    <div class="post-rt-block">
                        <div class="author-block">
                            {{#each user_details}}
                            <div class="author-block__name">{{this.username}}</div>
                            {{/each}}
                        </div>
                        <div class="author-block__date">{{moment date format="HH:mm"}}</div>
                    </div>                 
                    <ul class="flag-dropdown">
                        <li>   {{#if ispinned}}<i class="fa fa-map-pin" aria-hidden="true"></i> {{/if}}<i class="fa fa-ellipsis-h" aria-hidden="true" title="flag this post"></i>
                            <ul class="flag-dropdown__submenu">
                                       {{#if ../isAdmin}}
                                    <li id="{{this.post_id}}" class="{{#if ispinned}}pinned {{/if}}">    
                                                              
                                            {{#if ispinned}} 
                                            <label class="pin-checkbox-block"  id="{{this.post_id}}">
                                                <i class="fa fa-map-pin" aria-hidden="true"></i> <input type="checkbox" name="pinpostwall" class="pinpostwall pin-checkbox-block__checbox" checked/> 
                                                <span class="pin-checkbox-block__checkmark"></span>
                                                </label>
                                            {{else}}
                                                <label class="pin-checkbox-block"  id="{{this.post_id}}">
                                                <i class="fa fa-map-pin" aria-hidden="true"></i> <input type="checkbox" name="pinpostwall" class="pinpostwall pin-checkbox-block__checbox" /> 
                                                <span class="pin-checkbox-block__checkmark"></span>
                                                </label>
                                            {{/if}}                                   
                                    </li>
                                 {{/if}}  
                                <li id="{{this.post_id}}">
                                    <i class="fa fa-flag" aria-hidden="true"></i> <a href="javascript:void(0);" class="flag-post">Flag
                                        Post</a>
                                </li>
                            </ul>
                        </li>
                    </ul>
                    <!--  <a href="javascript:void(0);" class="flag-post"><i class="fa fa-ellipsis-h" aria-hidden="true" title="flag this post"></i></a>  -->
                </div>
                <div class="post-block">
                    <div class="post-block__description">{{this.description}}</div>
                    {{#if postimage}}<div class="post-block__featured-image"><img src="/images/{{this.postimage}}" /></div>{{/if}}
                </div>
            </div>
            {{/posts}}
        </div>
        <div id="infinite-loader" class="post-main-block posts-loader infinite-loader">
            <img src="images/loader.gif" alt="Loading . . ." />
        </div>
        <form method="post" action="/add" enctype="multipart/form-data" class="post-form post-form--desktop">
            <input type="text" name="description" placeholder="Post..." class="post-form__description" />
            <img src="images/clip.png" id="img" />
            <input type="file" name="postimage" class="post-form__file" id="file" placeholder="Write post...">
            <input name="submit" type="submit" value="send" class="post-form__button" />
        </form>
    </div>
    <form method="post" action="/add" enctype="multipart/form-data" class="post-form post-form--mobile">
        <input type="text" name="description" placeholder="Post..." class="post-form__description" />
        <img src="images/clip.png" id="img" />
        <input type="file" name="postimage" class="post-form__file" id="file" placeholder="Write post...">
        <input name="submit" type="submit" value="send" class="post-form__button" />
    </form>
    
    {{else}}
    {{> thanks }}
    {{/if}}
</div>